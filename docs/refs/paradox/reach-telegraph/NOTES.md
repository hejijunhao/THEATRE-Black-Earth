# Reach telegraph

Language to steal, not invent. Anchors: **Vic 3 selected-territory fill** and **Civ 6 movement range**.

## Steal

- **One silhouette.** Selected political geography / legal move range is a single filled blob. The outline is the union perimeter, not a plate, disc, or ring on every cell.
- **Wash leads; border supports.** Vic province colour sits *inside* the border. Civ 6 movement paint is a contiguous territory stain; the edge is a seam, not jewelry.
- **Fade stays inside the blob.** Distance / cost reads as a heart or gradient of the same region. It must not resolve into hex cells when you squint or amplify.

## Reject

- Overlapping `CircleGeometry` punches (r ≈ 0.94) that read as disc soup the moment the delta is amplified.
- Per-hex `RingGeometry` necklaces.
- Cool ink-white rims that outrank the soil stain.

## Status

Notes only until Vic 3 selection-territory and Civ 6 movement-range screenshots land in this folder. Do not invent replacement chrome while those shots are missing — keep the union polygon + supporting umber seam.

Frontline hatch stays under `board-chrome/` (HOI4 contact scar). This folder is **reach fill only**.
